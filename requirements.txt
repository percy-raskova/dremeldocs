# Core dependencies for astradocs
# Generated from pyproject.toml

# Core processing
ijson>=3.2.3              # Streaming JSON parser for large files

# MkDocs and documentation
mkdocs>=1.5.3             # Static site generator
mkdocs-material>=9.4.0    # Material theme for MkDocs
mkdocs-minify-plugin>=0.7.1  # Minification plugin
pymdown-extensions>=10.4  # Markdown extensions

# Utilities
click>=8.1.7              # CLI framework (future CLI support)
python-dateutil>=2.8.2    # Date parsing utilities
pyyaml>=6.0.1             # YAML configuration support

# Note: Removed obsolete dependencies:
# - anthropic, openai, tiktoken (AI/LLM - switched to local processing)
# - pandas, nltk, textstat (unused data processing)
# - aiohttp (unnecessary HTTP client)
# - loguru, tqdm, python-dotenv (moved to optional dependencies)
#
# To install optional dependencies:
# pip install astradocs[dev]     # Development tools
# pip install astradocs[logging]  # Enhanced logging
# pip install astradocs[extras]   # Progress bars and dotenv